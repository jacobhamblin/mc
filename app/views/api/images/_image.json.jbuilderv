json.extract! image, :id, :url, :description, :created_at, :updated_at

json.author do
  json.extract! image.author, :id, :username, :email, :avatar
end

json.subpack do
  json.extract! image.subpack, :id, :author_id, :title, :description, :url
end

json.pack do
  json.extract! image.pack, :id, :author_id, :title, :description, :url
end
